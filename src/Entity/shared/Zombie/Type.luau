--!strict
-- GERADO a partir do corpo. Nao editar a mao.
-- Folha: requer nada, para que dependencia mutua entre controllers funcione.
export type Public = {
	Heal: (self: Public, Amount: number) -> (),
}

return {}
